---
title: "התוכנה הראשונה ב-#C"
unit: "03-csharp-intro"
lesson: "csharp-start"
---

# ברוכים הבאים לעולם ה-#C! 🚀

עכשיו אנחנו עוברים לעולם ה-DotNet. שפה חזקה, מהירה ומקצועית המשמשת לבניית אפליקציות ענק.

## ההבדל העיקרי
בניגוד לפייתון, ב-#C אנחנו צריכים להגדיר "מחלקה" (Class) ו"פונקציית כניסה" (Main).

### דוגמה לקוד ב-#C:

<CodeRunner 
  language="csharp"
  initialCode={`using System;

public class Program {
    public static void Main() {
        Console.WriteLine("שלום מ-CSharp בסביבת ה-NextGen!");
    }
}`}
/>

במערכת החדשה, חלון ההרצה מזהה אוטומטית שמדובר ב-#C ומתאים את ה-Compiler.
